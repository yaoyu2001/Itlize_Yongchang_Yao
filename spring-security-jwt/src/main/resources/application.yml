spring:

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Rose890117$
    url: jdbc:mysql://localhost:3306/springSecurityDemo?serverTimezone=UTC

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: false
        globally_quoted_identifiers: true
server:
  port: 8081
